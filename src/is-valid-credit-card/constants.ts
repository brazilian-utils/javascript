/** De-facto industry minimum PAN (Primary Account Number) length (12, e.g. Maestro); ISO/IEC 7812-1 sets no such floor. */
export const MIN_LENGTH = 12;

/** Longest PAN (Primary Account Number) length allowed by ISO/IEC 7812-1, which caps the PAN at 19 digits. */
export const MAX_LENGTH = 19;
