// Code generated by the logic engine. DO NOT EDIT.
// engine: 0.1.0
// source: lib/digits
// content: 780bb464b89e

package core

import (
	"strings"
)

// Keeps only the ASCII digits and letters of a value, upper casing the letters.
func keepAlphanumeric(value string) string {
	return strings.ToUpper(func() string {
		__value := value
		__out := make([]byte, 0, len(__value))
		for __i := 0; __i < len(__value); __i++ {
			__b := __value[__i]
			__c := int(__b)
			if (__c >= 48 && __c <= 57) || (__c >= 65 && __c <= 90) || (__c >= 97 && __c <= 122) {
				__out = append(__out, __b)
			}
		}
		return string(__out)
	}())
}

// Keeps only the ASCII digits of a value, dropping every mask character.
func keepDigits(value string) string {
	return func() string {
		__value := value
		__out := make([]byte, 0, len(__value))
		for __i := 0; __i < len(__value); __i++ {
			__b := __value[__i]
			__c := int(__b)
			if __c >= 48 && __c <= 57 {
				__out = append(__out, __b)
			}
		}
		return string(__out)
	}()
}

// Whether every scalar of the value is the same one, for whatever length the caller proved —
// `isRepeated` and `isRepeatedCnpj` do the same check for one specific length; this one serves a
// generator that has to run it on a base shorter than the document it is building.
func isRepeatedRun(value string) bool {
	first := int(value[0])
	for index := 1; index < len(value); index++ {
		if int(value[index]) != first {
			return false
		}
	}
	return true
}

// The numeric value of one ASCII digit.
func digitAt(value string, index int) int {
	return (int(value[index]) - 48)
}

// The numeric value of one ASCII digit.
func digitAt2(value string) int {
	return (int(value[9]) - 48)
}

// The numeric value of one ASCII digit.
func digitAt3(value string) int {
	return (int(value[10]) - 48)
}
