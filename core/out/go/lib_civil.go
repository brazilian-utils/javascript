// Code generated by the logic engine. DO NOT EDIT.
// engine: 0.1.0
// source: lib/civil
// content: c2aca1eb50dc

package core

// A fixed day of a year, with the unreachable fallback named once.
func civilDate(year int) int {
	return orElse(ymdToDays(year, 1, 1), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate1(year int) int {
	return orElse(ymdToDays(year, 4, 21), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate2(year int) int {
	return orElse(ymdToDays(year, 5, 1), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate3(year int) int {
	return orElse(ymdToDays(year, 9, 7), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate4(year int) int {
	return orElse(ymdToDays(year, 10, 12), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate5(year int) int {
	return orElse(ymdToDays(year, 11, 2), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate6(year int) int {
	return orElse(ymdToDays(year, 11, 15), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate7(year int) int {
	return orElse(ymdToDays(year, 12, 25), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate8(year int) int {
	return orElse(ymdToDays(year, 11, 20), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate9(year int, day int) int {
	return orElse(ymdToDays(year, 3, day), min(max(0, -719162), 2932896))
}

// A fixed day of a year, with the unreachable fallback named once.
func civilDate10(year int, day int) int {
	return orElse(ymdToDays(year, 4, day), min(max(0, -719162), 2932896))
}
