# Code generated by the logic engine. DO NOT EDIT.
# engine: 0.1.0
# source: std/strings
# content: f790963d50f6

from typing import List


def ascii_upper_all(value: str) -> str:
    """ASCII-only upper casing, for values that are not proven ASCII."""
    points: List[int] = []
    for point in [ord(__c) for __c in value]:
        if (point >= 97) and (point <= 122):
            points.append((point - 32))
        else:
            points.append(point)
    return "".join(chr(__p) for __p in points)
