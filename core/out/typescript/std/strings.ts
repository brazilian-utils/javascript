// Code generated by the logic engine. DO NOT EDIT.
// engine: 0.1.0
// source: std/strings
// content: f790963d50f6
/**
 * ASCII-only upper casing, for values that are not proven ASCII.
 */
export function asciiUpperAll(value: string): string {
	let points: number[] = [];
	for (const point of Array.from(value, (scalar) => scalar.codePointAt(0)!)) {
		if (point >= 97 && point <= 122) {
			points.push(point - 32);
		} else {
			points.push(point);
		}
	}
	return points.map((point) => String.fromCodePoint(point)).join("");
}
