// Code generated by the logic engine. DO NOT EDIT.
// engine: 0.1.0
// source: lib/digits
// content: d2311b4b59ec
/**
 * Keeps only the ASCII digits and letters of a value, upper casing the letters.
 */
export function keepAlphanumeric(value: string): string {
	return value.replace(/[^0-9A-Za-z]/gu, "").toUpperCase();
}

/**
 * Keeps only the ASCII digits of a value, dropping every mask character.
 */
export function keepDigits(value: string): string {
	return value.replace(/[^0-9]/gu, "");
}

/**
 * The numeric value of one ASCII digit.
 */
export function digitAt(value: string, index: number): number {
	return value.charCodeAt(index) - 48;
}
